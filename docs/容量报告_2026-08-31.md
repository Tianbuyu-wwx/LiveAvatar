# 容量报告（自研 WS 传输，CPU 合成 worker）

> 口径说明：本报告由 `scripts/capacity_report.py` 脚手架生成，运行于 **CPU 合成
> worker**（128² 动画图案，无真实 MuseTalk 推理），验证的是三路并发下的**调度、
> 隔离与传输正确性**（独立 seq/epoch、打断互不串扰、无饥饿）。单卡 GPU 真实
> 容量（512² MuseTalk 会话数上限）需在 GPU 空闲时以同一脚本对真实 worker 重跑。

- 并发会话数：3
- 每会话时长：6 s
- 总出口带宽：1.71 Mbps

| 会话 | fps | 码率 kbps | gap p95 ms | drift p95 ms | 启动 ms | 打断 ms |
|---|---|---|---|---|---|---|
| sess_085f78e9843131e4 | 40.6 | 570 | 107.56 | -212.4 | 11.6 | 4.0 |
| sess_2d726bd1f7e74296 | 40.6 | 570 | 107.68 | -215.0 | 11.2 | 4.1 |
| sess_aaf522f23989c0b6 | 40.6 | 570 | 107.63 | -216.9 | 8.6 | 3.5 |

## 门禁

- 单会话 fps ≥ 20.0：实测最低 40.6 → 通过
- 打断延迟 ≤ 90.0 ms（p95）：实测 4.1 ms → 通过
- 零饥饿会话：0 个空会话 → 通过
